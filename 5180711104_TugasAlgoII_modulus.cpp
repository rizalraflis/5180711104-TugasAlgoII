#include <iostream>
#include <conio.h>
using namespace::std;

int mdls(int nilai,int x){
    if
        (x==0)
        {return (nilai%x);}
    else if
        (x==nilai)
        {return (0);}
    else
        {return (nilai%x);}
    }


main()
    {
    int y,z;
    cout<<"masukan bilangan 1: ";
    cin>>y;
    cout<<"masukan bilangan 2: ";
    cin>>z;
    cout<<"hasil sisa :";
    cout<<mdls(y,z);
    }
